# Runtime dependencies for the DigiDARA FastAPI orchestrator.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
typing-extensions>=4.12,<5
SQLAlchemy>=2.0,<3
PyMySQL>=1.1,<2
python-dotenv>=1.0,<2
httpx==0.27.2
# These pins match the working Capstone dependency set. LiteLLM 1.55.4
# requires httpx 0.27.x; newer releases can fail metadata resolution on
# legacy Windows/Python environments.
litellm==1.55.4
langgraph==0.2.62
bcrypt>=4.2,<5
PyJWT>=2.9,<3
slowapi>=0.1.9,<1
# Production process manager target (see ecosystem.config.js) — not used by
# `uvicorn --reload` in local dev, and doesn't run as a server on Windows.
gunicorn>=23.0,<24
