import ModernResumeTemplate from "./ModernResumeTemplate.jsx";

export default function TimelineTealTemplate({ resume }) {
  return <ModernResumeTemplate resume={resume} variant="timeline-teal" twoColumn />;
}
