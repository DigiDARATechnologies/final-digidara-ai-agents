# Copy to frontend/.env for local development. Only VITE_-prefixed variables
# are exposed to the browser bundle - never put backend secrets here.
VITE_API_BASE_URL=/api
VITE_PROXY_API_TARGET=http://localhost:5000
VITE_DEV_USER_ID=
VITE_API_TIMEOUT_MS=15000
