# Completed rebuild

## Secure workflow and ATS release

- Added a server-issued HTTP-only guest session and CSRF protection; untrusted browser user IDs are disabled by default.
- Added rate limits for AI generation, import, ATS analysis, preview, and PDF export.
- Unified TXT, PDF, DOCX, and legacy DOC analysis through the same validated backend flow.
- Added saved-resume ATS analysis with transparent category scoring and weighted job-keyword coverage.
- Persisted ATS score, job match score, analysis time, PDF export count, and declaration text.
- Added debounced autosave, saved-state feedback, an ATS-aware dashboard, and a guided four-stage workflow.
- Added an original premium landing and dashboard treatment based on guided resume-builder best practices.
- Removed the vulnerable routing dependency and replaced it with a minimal first-party history router.
- Added database verification, secure-session tests, ATS persistence tests, and a security audit report.

- Replaced the application shell, landing page, dashboard, editor, preview workspace, cards, controls, and mobile breakpoints with the ResumeForge AI design system.
- Consolidated the gallery and backend catalog to exactly four distinct templates.
- Replaced browser canvas export with server-side selectable-text PDF export.
- Added an exact live PDF preview generated by the same renderer as downloads.
- Standardized PDF generation on ReportLab for consistent Windows, Linux, and macOS behavior.
- Removed unused template implementations and client PDF dependencies.
- Added owner-context enforcement, security headers, upload/request limits, field validation, AI input limits, and optional AI SDK startup handling.
- Added local AI fallbacks, one-click Windows startup, updated documentation, and automated coverage for preview and access controls.
