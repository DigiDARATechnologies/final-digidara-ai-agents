# http://127.0.0.1:8100 only works when this agent runs directly on the
# host. Any copy of this template deployed into docker-compose.yml must
# point at the orchestrator's Docker service name instead.
ORCHESTRATOR_URL=http://orchestrator:8100
HEARTBEAT_INTERVAL_SECONDS=30
