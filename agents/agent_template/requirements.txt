fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
python-dotenv==1.0.1
# Production/Docker process target — not used by `uvicorn --reload` in local
# dev, and doesn't run as a server on Windows.
gunicorn==23.0.0
