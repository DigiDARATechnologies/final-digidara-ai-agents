export default function ExitInterviewModal({ continueButtonRef, exitButtonRef, onContinue, onConfirm, isExiting }) {
  return <div className="modal-backdrop" role="presentation"><div className="confirm-modal" role="dialog" aria-modal="true" aria-labelledby="exit-modal-title" aria-describedby="exit-modal-description"><h2 id="exit-modal-title">Exit Interview?</h2><p id="exit-modal-description">Are you sure you want to end this interview? Your current progress may be saved, but the interview will be marked as incomplete.</p><div className="modal-actions"><button ref={continueButtonRef} className="secondary-btn" onClick={onContinue} disabled={isExiting}>Continue Interview</button><button ref={exitButtonRef} className="danger-btn" onClick={onConfirm} disabled={isExiting}>{isExiting ? "Exiting interview..." : "Exit Interview"}</button></div></div></div>;
}
