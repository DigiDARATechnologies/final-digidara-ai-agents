# Copy to docker/mysql/.env (gitignored — matches the repo's existing
# `.env` / `.env.example` convention) and set a real password before running
# `docker compose up -d`. Referenced only by the mysql service in the root
# docker-compose.yml — never baked into any image.
MYSQL_ROOT_PASSWORD=change-me
